import Foundation

let firstCard = 11
let secondCard = 10

print(firstCard == secondCard ? "Cards are the same" : "Cards are different")

let isAuthenticated = false
print(isAuthenticated ? "Welcome" : "Who are you")

let storageSpace = 500
print(storageSpace > 1000 ? "Success" : "Failure")

let isRecylable = false
print(isRecylable == true ? "Succees" : "Failure")
